//  const Base64 = require("js-base64").Base64;
//  import { Base64 } from "js-base64";
//编码
// let param={"tenant":"default","name":"admin","password":"123456","version":"Baymax-3.0.0.23-20180606"};

// let url ="http://localhost:8080/Baymax#/dataPanorama?"+encodeURIComponent(Base64.encode(JSON.stringify(param)));

//解码
// let params = JSON.parse(
//     Base64.decode(decodeURIComponent(href.substring(href.indexOf("?")+1,href.length)))
// );
